server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/HoussamEddineSmati/TravelAl
          clone-on-start: true
eureka:
  client:
    service-url:
      defaultZone=http://localhost:8761/eureka/